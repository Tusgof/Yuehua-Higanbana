# IMPLEMENT_PLAN.md

## Overview
- **Start state**: Higanbana has a working SPY 0DTE research scaffold, Mar-Dec 2023 in-sample and Jan-Dec 2024 OOS option data, 90 Sub-System A closed trades, Sub-System B feasibility evidence, M4/M5 diagnostic reports, macro/VIX inputs, OPRA OI feasibility/enrichment probes, and one gamma aggregation diagnostic. All current strategy evidence is tier E1 diagnostic at best: under-sampled, underpowered, search-contaminated where applicable, and not deployable.
- **End state**: A hypothesis-led research system that can say which strategy hypotheses are falsified, parked, validated for operational paper/dry-run, or still blocked. Paper trading is allowed only after E2 research acceptance, or as an explicitly labeled E0 operational dry run with no edge claim. Real-money launch remains a separate blocked gate requiring IBKR permission, account feasibility, explicit user launch instruction, kill switch, and final checklist pass.
- **Total tracks**: 7
- **Estimated total effort**: XL

---

## Current Locked Decisions
- Technical DD Workstream 1 is complete. The user lifted the paid-data and hypothesis-expansion freeze effective 2026-07-12 after the interim Google Drive restore passed and the aggregate pilot registry mismatch was resolved by append-only snapshot supersession.
- Future purchases remain restricted by the data-acquisition decision tree in `docs\FABLE5_UPGRADE_PROPOSAL.md` section 5. The next approved candidates remain targeted H-G1 OI dates (about `$4.30`) and the 2022 H2 top-VIX months (about `$14-21`), each still requiring its named hypothesis gap, live cost estimate, selected key, and cost guard to pass.
- Normal in-plan SPY-only Higanbana work uses a proceed-by-default rule. Agents state intent, affected files, and verification path, then continue without asking for approval.
- Do not ask for approval as a routine workflow step. Ask a question only when required information is missing and cannot be inferred safely. Hard-stop boundaries are out-of-plan safety stops, not approval prompts for ordinary work.
- Edge validation comes before paper trading.
- Experiment numbers are legacy identifiers, not execution order.
- Existing Databento working usage is `$120.494368` against the legacy `$125` guard. User added `DATABENTO_API_MO` and `DATABENTO_API_AI`; use them as one approved `$200` research pool while keeping each individual key below its `$100` maximum-use cap. The selected key env must be logged explicitly before paid action.
- Do not store Databento key values. Multi-account signup-credit harvesting remains prohibited.
- No broad calendar buying. Every data pull must serve a named hypothesis and a named field, regime, window, or falsification gap.
- No live LLM research calls until real timestamp-clean news cases exist.
- No current result may be treated above E1 diagnostic evidence.
- Every hypothesis/report conclusion must use the project labels `ผ่าน`, `ไม่ผ่าน`, or `ยังสรุปไม่ได้`, with evidence tier and blockers stated beside the conclusion.
- Latest experiment research log is `040-higanbana-h-a2-breakeven-aware-train-diagnostic.md`; logs through `040` are pushed to the Yuehua Research Log repo. Recent logs were rewritten as easier Thai research notes after user feedback, preserving the original evidence, metrics, artifact paths, and conclusions. `RESEARCH_LOG_FORMAT.md` now explicitly requires human-readable Thai, English only for necessary technical terms, and no machine-log style writing.

---

## Workstream 1: Clean-Clone Self-Verification
**Goal**: Make the repository verifiable on a clean non-author machine before further paid acquisition or hypothesis expansion.
**Dependencies**: none
**Priority**: Critical; temporarily supersedes research-track execution.

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| DD1.1 | [DONE] Split tests into `hermetic` and `state-audit`; missing local roots skip with an explicit root name | L | RISK | Linux CI exposed eight additional state-dependent tests; corrected hermetic tier now contains 535 tests |
| DD1.2 | [DONE] Add `HIGANBANA_DATA_ROOT`, `HIGANBANA_WIKI_ROOT`, and `HIGANBANA_IBKR_PYTHON` environment resolution with untracked `machine.json` fallback | M | RISK | Hermetic forbidden-path test passes for `scripts/`, `tests/`, and `experiments/` |
| DD1.3 | [DONE] Fix cross-platform path assertions, declare supported Python and optional pinned Databento dependency, and standardize interpreter metadata for new reports | M | RISK | Portability, environment, and Python-version tests pass |
| DD1.4 | [DONE] Add GitHub Actions for the hermetic tier and expose CI status in the control plane | S | RISK | Linux GitHub Actions run `29002864982` attempt 2 passed all 535 hermetic tests on commit `ea6092e` |
| DD1.5 | [DONE] Document backup/restore for the four project stores and add paid-data checksum verification | L | RISK | Dual hashes cover 6,626 paid artifacts; the restored cloud copy passes with zero blockers, including two accepted `content_verified_envelope_variance` files |
| DD1.6 | [DONE] Rehearse clean-clone and data restore verification | L | RISK | Fable clean-clone hermetic 535/535 passed; the 2026-07-12 interim cloud restore reproduced 6,785 files / 31,794,213,413 bytes and passed restored-copy integrity |

**Current exit blockers**: none. The physical off-site copy remains a non-blocking follow-up when media arrives. The historical aggregate snapshot remains immutable and is superseded by the later same-day snapshot whose hash matches the restored directory.

**2026-07-10 Fable/Codex follow-up**: Fable independently verified WS1 hermetic clean clone (`535/535`). Codex then resolved the two 2024-06-13 exit-check files by canonical DBN record-body plus canonical record-export comparison: both current/fresh pairs have identical 90,700-record content despite differing metadata/container hashes. `docs\DATABENTO_INTEGRITY_POLICY.md` and `scripts\verify_data_integrity.py` now enforce dual hashes; no historical hash was rebaselined. OPRA statistics stat-type mapping is hermetic for known fixture values (`OPEN_INTEREST=9`, `CLOSE_PRICE=11`) instead of relying on the Databento package.

---

## Workstream 2: Golden Statistics Anchors
**Goal**: Make load-bearing statistics conventions testable with fixed golden numbers.
**Dependencies**: Workstream 1 local remediation
**Priority**: High

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| DD2.1 | [NOT STARTED - FABLE ORDER] Add a shared statistics kernel for new code without migrating frozen scripts | M | RISK | Start only after WS1 exit; prior Codex local scaffolds are not Fable-accepted |
| DD2.2 | [NOT STARTED - FABLE ORDER] Add hermetic golden-number tests for statistics conventions | M | RISK | Start only after WS1 exit; prior Codex local scaffolds are not Fable-accepted |

**Track complete when**: New statistics code has fixed golden anchors. Existing frozen reports are not rewritten by this track.

---

## Workstream 3: Two-Zone Code Rule
**Goal**: Keep new hypothesis-independent infrastructure in `lib/` while preserving frozen historical scripts.
**Dependencies**: Workstream 1 local remediation
**Priority**: High

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| DD3.1 | [DONE] Complete shared `lib/` helpers for new code | S | RISK | `lib/io.py`, `timestamps.py`, `guardrails.py`, `search_log.py`, `report.py`, `regime_inputs.py`, and `statistics.py` have hermetic unit coverage; existing experiment scripts were not migrated |
| DD3.2 | [DONE] Add helper drift audit without auto-fixing frozen scripts | M | RISK | `scripts/audit_helper_drift.py` runs in state-audit and fixture pipeline, writes `reports\diagnostics\helper_drift_audit.json`, and reports only findings |
| DD3.3 | [DONE] Add readiness metric for new scripts bypassing `lib/` | S | RISK | `scripts/audit_new_script_lib_usage.py` runs before readiness; `new_script_lib_usage.bypassing_lib_count=0` is surfaced in `research_readiness_audit` |

**Track complete when**: New code uses `lib/`, drift is measured, and readiness reports helper-bypass count.

---

## Workstream 4: Gate Integrity
**Goal**: Prevent silent mutation of locked preregistration artifacts and validators.
**Dependencies**: Workstream 1 local remediation
**Priority**: High

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| DD4.1 | [DONE] Seed append-only locked-gate manifest | M | RISK | `experiments\locked_gates.jsonl` records decision/validator SHA-256 pairs for the three designated active governance locks |
| DD4.2 | [DONE] Add hermetic locked-gate hash validation test | S | RISK | `tests\test_validate_locked_gates.py` validates current hashes, rejects hash drift, accepts only human-reviewed supersession, and rejects missing human approval |
| DD4.3 | [DONE] Add locked validator review and agent-version rule to `AGENTS.md` | S | RISK | `AGENTS.md` requires user/Fable review for locked gate changes and an `Agent: model/version` commit trailer; CI rejects a `main` push whose HEAD lacks it |
| DD4.4 | [DONE] Add adversarial-review step to E2 promotion checklist | S | RISK | `docs\EVIDENCE_TIER_POLICY.md` requires independent adversarial/refutation review and `scripts\evaluate_research_acceptance.py` enforces `adversarial_review_completed` |

**Track complete when**: Complete. Locked gates are hash-verified in the hermetic tier and fixture pipeline. Revisions require an append-only, human-reviewed supersession entry; the three-entry manifest floor prevents silent seed deletion; E2 promotion requires adversarial review.

---

## Workstream 5: Evolution Hygiene
**Goal**: Reduce long-term artifact drift without deleting evidence.
**Dependencies**: Workstream 1 local remediation
**Priority**: Medium

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| DD5.1 | [DONE] Approve report retention policy and produce the first dry-run manifest without archival | S | RISK | User selected `archive/reports-YYYY`, immutable JSON index on `main`, Bulk diagnostic scope, and cutoff 2026-06-30. `reports\diagnostics\report_retention_dry_run_2026_07_12.json` reports zero eligible files because report history begins 2026-07-03; no files moved |
| DD5.2 | [DONE] Enforce wiki citation hashes for active preregistrations | M | RISK | Companion registry binds 2 active preregistrations and 9 citations; state-audit fails on artifact/wiki drift without mutating locked or frozen artifacts |
| DD5.3 | [DONE] Add OPRA statistics ingest schema validation | M | RISK | Boundary schema pins dataset/schema, required columns, stat-type mapping, and OPEN_INTEREST; the Databento probe blocks research use on drift |
| DD5.4 | [DONE] Start governance epoch tagging | S | RISK | Manifest records `technical-dd-remediation-ws5-2026-07-11`; annotated tag is pushed with the WS5 evidence commit |

**Track complete when**: Complete. Policy approval and dry-run evidence exist. Any future non-empty archive execution remains a separate reviewed step with SHA-256 restore verification.

---

## Track P-1: Cost-Basis Sync
**Goal**: Make paid-action decisions use the real cost guard before any new purchase.
**Dependencies**: none
**Status**: Complete for the current `$105` update.

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| P-1.1 | Update `reports\data_cost\user_reported_actual_usage.json` to actual usage about `$105` | S | RISK | `python scripts\audit_paid_costs.py` reports `cost_guard_used_usd=105.0` |
| P-1.2 | Add budget policy: cap extension by real payment on the existing Databento account only; prohibit multi-account credit harvesting | S | RISK | `reports\data_cost\paid_cost_audit.md` includes the Budget Policy section |
| P-1.3 | Keep conservative committed estimates as trace evidence, not the controlling guard while actual usage exists | S | OK | `paid_cost_audit.json` includes actual-usage and known-estimate reconciliation |

**Track complete when**: Paid-cost audit passes on actual-usage basis and reports current remaining headroom under the `$125` guard.

---

## Track P0: Registry, Evidence Tiers, And Audit Backbone
**Goal**: Move the project from experiment-number sequencing to hypothesis-led execution with machine-checkable evidence tiers and next actions.
**Dependencies**: Track P-1

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| P0.1 | Create `docs\HYPOTHESIS_REGISTRY.md` with H-A1, H-A2, H-B1, H-B2, H-G1, H-L1, and H-L2 seed entries | M | RISK | Complete: document exists and records rationale, predictions, validation/falsification criteria, required data, evidence links, decision logs, and the kill/resurrection rule |
| P0.2 | Create `experiments\hypothesis_registry.json` as the machine-readable registry | M | RISK | Complete: registry includes the original seven hypotheses plus H-L3 market-outcome text filtering; `python scripts\validate_hypothesis_registry.py` must report `status=pass`, `hypothesis_count=8` |
| P0.3 | Add `scripts\validate_hypothesis_registry.py` and tests | M | RISK | Complete: `python -m unittest tests.test_validate_hypothesis_registry` passes |
| P0.4 | Add evidence-tier policy and validator requiring `hypothesis_id`, `evidence_tier`, and `tier_blockers` for experiment summaries | L | RISK | Complete: `python scripts\validate_evidence_tiers.py` passes; tests reject unknown hypothesis IDs, missing metadata on acceptance claims, and acceptance claims below E2 |
| P0.5 | Fix stale `scripts\audit_research_readiness.py` next action after gamma diagnostic exists | M | RISK | Complete: `python -m unittest tests.test_audit_research_readiness` passes and readiness next action now points to H-G1/H-A2 targeted next steps instead of rerunning the completed gamma diagnostic |
| P0.6 | Add read-only `scripts\report_project_state.py` | M | OK | Complete: command reports hypothesis status, tier, blockers, next safe action, cost headroom, and news/GDELT status with no network calls or file writes |
| P0.7 | Wire registry/tier validators into `scripts\run_fixture_pipeline.py` | S | RISK | Complete: `python scripts\run_fixture_pipeline.py` passes and includes `validate_hypothesis_registry` plus `validate_evidence_tiers` |

**Track complete when**: Complete. One command can report current project state from the registry and audits, and no experiment artifact can silently claim acceptance-grade evidence without E2 fields.

---

## Track H-A2: Macro-Conditioned ORB Edge
**Goal**: Turn the best current diagnostic clue into a pre-registered, falsifiable hypothesis instead of continuing blind data buying.
**Dependencies**: Track P0
**Priority**: Highest edge track.

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| H-A2.1 | Register H-A1 as falsified-as-stated with a mechanism autopsy, and H-A2 as the new macro-conditioned hypothesis | M | RISK | Registry validator passes and H-A1/H-A2 decision logs are explicit |
| H-A2.2 | Re-analyze the existing M5.5 macro-filter result as H-A2 E1 evidence with inherited 9-trial search contamination and DSR blocker/adjustment | M | RISK | Complete: `reports\experiments\h_a2_macro_conditioned_reanalysis_summary.json` cites the 9 M5.5 trials, labels evidence `E1`, records DSR/sample/regime blockers, and does not claim E2 |
| H-A2.3 | Investigate why cached Aug 2024 VIX-spike windows produced zero high-VIX trades | M | RISK | Complete: `reports\diagnostics\h_a2_high_vix_silence_diagnostic_summary.json` shows complete VIX/market-data coverage and zero ORB candidates on high-VIX dates, so the gap is ORB silence rather than a labeling gap |
| H-A2.4 | If still justified, estimate 2022 H2 top 2-3 VIX months only, before any purchase | S | RISK | Complete: `reports\data_cost\h_a2_2022_h2_stress_purchase_estimate.json` ranks 2022-10 and 2022-09 as top2, estimates base cost `$16.923156` inside `$20` headroom, and marks top3 as requiring live cost or top-up |
| H-A2.5 | Buy 2022 H2 stress data only if the decision tree passes and headroom/top-up allows it | L | RISK | Scope revised and partially complete: top2 remains blocked, but `experiments\h_a2_2022_10_single_month_stress_decision.json` approved 2022-10 only; `reports\data_cost\databento_download_result_h_a2_2022_10_stress.json` downloaded 21 OPRA `cbbo-1m` daily-union files and `reports\data_cost\databento_normalization_summary_h_a2_2022_10_stress.json` normalized 1,516,757 0DTE quote rows. H-A2 rerun is still blocked because Databento `EQUS.MINI` cannot supply 2022-10 SPY bars |
| H-A2.7 | Resolve 2022-10 SPY underlying bar source before rerunning H-A2 stress diagnostics | M | RISK | Complete: IBKR readiness passed on TWS port `7496`; `scripts\download_h_a2_2022_ibkr_spy_bars.py` downloaded 5,070 SPY one-minute bars for all 13 target dates in read-only mode with no orders. Every date has 390 regular-session rows and the 09:35/15:45 timestamps. Raw and canonical hashes are registered and verified separately |
| H-A2.7a | Price and execute the staged 2022 H2 stress block including real SPY bars | S | RISK | Complete for stage 1: user approved the October block; cached October option quotes cost `$0`, OI was not required, and IBKR returned the 13 SPY bar dates with `$0` observable per-request charge against a projected `$1.50` subscription cost. The subscription charge is not observable through the API. The contingent September quote purchase is stopped because the October trade-density checkpoint observed `0` candidates against minimum `2` |
| H-A2.8 | Reframe missing 2022 SPY 1-minute bars as an exact-backtest blocker, not a full hypothesis blocker | S | RISK | Complete: `docs\HYPOTHESIS_DATA_RESOLUTION_AUDIT.md` separates mechanism, proxy, exact-backtest, and deployable evidence tiers. It states that 2022 SPY 1-minute bars block only the exact 2022-10 ORB rerun; no-paid coarse stress/regime review or a pre-registered lower-resolution proxy may proceed with downgraded E1 claims |
| H-A2.9 | Pre-register the 2022-10 coarse stress/regime review before running it | S | RISK | Complete: `experiments\h_a2_2022_10_coarse_stress_review_preregistration.json` and `docs\H_A2_2022_10_COARSE_STRESS_REVIEW_PREREGISTRATION.md` define a no-paid, no-network, no-IBKR, no-LLM, no-PnL trading-day review using VIX, macro calendar, and 0DTE quote availability only. `python scripts\validate_h_a2_2022_10_coarse_stress_preregistration.py` verifies guardrails and planned outputs |
| H-A2.10 | Run the 2022-10 coarse stress/regime review using only already-downloaded local inputs | M | RISK | Complete: `reports\diagnostics\h_a2_2022_10_coarse_stress_review.json` reports E1 `continue_exact_rerun_research`: 21 trading days, 13 quote-available 0DTE days, all 13 overlapping high/stress VIX, and 3 quote-available high-importance macro days. No network, paid data, IBKR, LLM, strategy PnL, exact ORB, or paper-trading approval was used. Research log `021-higanbana-h-a2-2022-10-coarse-stress-review.md` was written locally |
| H-A2.11 | Pre-register the H-A2 follow-up data-resolution decision before any lower-resolution proxy test or exact-source chase | S | RISK | Complete: `experiments\h_a2_followup_resolution_preregistration.json` and `docs\H_A2_FOLLOWUP_RESOLUTION_PREREGISTRATION.md` select `lower_resolution_proxy_first` as the next path, forbid IBKR, paid data, new providers, strategy PnL, paper trading, and exact-source chase from H-A2.10 alone. `python scripts\validate_h_a2_followup_resolution_preregistration.py` passes |
| H-A2.12 | Pre-register the actual lower-resolution proxy test before running it | S | RISK | Complete: `experiments\h_a2_lower_resolution_proxy_preregistration.json` and `docs\H_A2_LOWER_RESOLUTION_PROXY_PREREGISTRATION.md` lock the H-A2 mechanism-first proxy test, 5-minute and 15-minute resolutions, existing trade-outcome reconciliation, chronological split, no OOS tuning, DSR/search-log, MinTRL/PSR, big-day dependency, and no paid/IBKR/LLM/paper-trading permissions. `python scripts\validate_h_a2_lower_resolution_proxy_preregistration.py` passes |
| H-A2.13 | Run the pre-registered lower-resolution proxy test using only local data | M | RISK | Complete: `scripts\run_h_a2_lower_resolution_proxy.py` generated `reports\experiments\h_a2_lower_resolution_proxy_summary.json`, `.md`, and `reports\experiments\search_logs\h_a2_lower_resolution_proxy_search_log.jsonl`. Result is E1 `ยังสรุปไม่ได้`: 444 measured proxy days from 463 daily rows, 7 pre-registered trials, 5-minute and 15-minute proxies plus existing trade outcomes are directionally coherent with non-risk days outperforming risk-labeled days, but no exact 2022 ORB, no E2 edge claim, no paid data, no IBKR, no LLM, and no paper trading approval |
| H-A2.14 | Decide whether H-A2.13 justifies exact-data planning before any source execution | S | RISK | Complete: `experiments\h_a2_exact_data_prioritization_decision.json` and `docs\H_A2_EXACT_DATA_PRIORITIZATION_DECISION.md` select `draft_narrow_exact_2022_underlying_bar_plan` as the next path. `python scripts\validate_h_a2_exact_data_prioritization_decision.py` passes. The decision is E0 only: H-A2.13 is directionally coherent enough to justify a narrow 2022-10 underlying-bar plan, but it does not approve IBKR requests, paid data, new providers, LLM calls, strategy PnL, paper trading, operational validation, real-money trading, or an H-A2 edge claim |
| H-A2.15 | Draft the narrow exact 2022 underlying-bar acquisition/import plan before any source execution | S | RISK | Complete: `experiments\h_a2_exact_2022_underlying_bar_plan.json` and `docs\H_A2_EXACT_2022_UNDERLYING_BAR_PLAN.md` define the 2022-10 SPY 1-minute bar scope, 21-day window, 13 quote-available target dates, source execution order, validation gates, and forbidden actions. `python scripts\validate_h_a2_exact_2022_underlying_bar_plan.py` passes. The plan is E0 only: next is to implement and test a bounded acquisition/import tool in dry-run and fixture mode, not to execute live IBKR requests, buy data, introduce a new provider, rerun exact stress diagnostics, or claim edge |
| H-A2.16 | Implement and test the bounded SPY 2022-10 bar acquisition/import tool in dry-run and fixture mode | M | RISK | Complete: `scripts\acquire_h_a2_2022_spy_bars.py` writes the source manifest, coverage audit, import summary, Markdown report, and fixture canonical `spy_bar` output under `build\h_a2_2022_spy_bars_fixture\spy_bar.jsonl`. `reports\data_cost\h_a2_2022_spy_bars_acquisition_manifest.json`, `reports\diagnostics\h_a2_2022_spy_bars_coverage_audit.json`, and `reports\data_cost\h_a2_2022_spy_bars_import_summary.json` confirm 21 fixture trading days, 8,211 canonical rows, 13/13 option-join target dates covered, ET timestamp conversion, no network, no paid data, no IBKR historical request, no orders, no strategy PnL, and `ready_for_exact_rerun=false`. `tests\test_acquire_h_a2_2022_spy_bars.py`, `tests\test_audit_research_readiness.py`, `tests\test_validate_evidence_tiers.py`, and `tests\test_fixture_pipeline.py` pass. Next is IBKR readiness/live data-only gate, not exact rerun or edge claim |
| H-A2.17 | Rerun the IBKR readiness gate after H-A2.16 before any live data-only request | S | RISK | Complete and externally blocked: `reports\diagnostics\ibkr_spy_bars_readiness_probe_h_a2_2022_10.json` and `.md` report `blocked_local_ibkr_unavailable`. `ib_insync` and `ibapi` are available through `D:\Fogust\Workspace\Investment\Project\Yuehua Investment Lab\.venv\Scripts\python.exe`, but no local IBKR API port is listening on 7497/7496/4002/4001. Guardrails confirm no historical data request, no order transmission, no paid data, no new provider, and no paper/real-money approval. Next is to start local TWS/Gateway with API enabled and market-data permission, then rerun this readiness probe; otherwise stop before Alpaca or any new paid provider |
| H-A2.18 | Pre-register a second proxy-first H-A2 robustness path before chasing 1-minute bars again | S | RISK | Complete: `experiments\h_a2_proxy_first_robustness_preregistration.json` and `docs\H_A2_PROXY_FIRST_ROBUSTNESS_PREREGISTRATION.md` state that 1-minute bars are required for exact entry/exit replay, not for mechanism-level proxy falsification. The plan locks daily-regime, 15-minute, 5-minute, and existing exact-trade reconciliation tests; requires chronological split, sample counts, MinTRL/PSR, DSR/search-log handling, and big-day dependency where applicable; and forbids E2 claims, paper trading, operational validation, IBKR requests, new paid data, new providers, live LLM calls, and exact 2022-10 ORB claims |
| H-A2.19 | Run the preregistered proxy-first H-A2 robustness test using local/no-paid inputs only | M | RISK | Complete: `scripts\run_h_a2_proxy_first_robustness.py` generated `reports\experiments\h_a2_proxy_first_robustness_summary.json`, `.md`, and `reports\experiments\search_logs\h_a2_proxy_first_robustness_search_log.jsonl`. Result is E1 `ยังสรุปไม่ได้`: 463 daily rows, 444 measured proxy days, 90 existing trade days, non-risk minus risk is positive across 5-minute proxy (`0.001646`), 15-minute proxy (`0.000669`), and existing implementable trade outcomes (`23.375`). `scripts\validate_h_a2_proxy_first_robustness_summary.py` and `tests\test_h_a2_proxy_first_robustness.py` pass. No network, paid data, broker request, LLM call, new provider, exact 2022 replay, paper trading, operational validation, or real-money approval was used |
| H-A2.20 | Decide the next H-A2/H-L1 path after proxy robustness and macro-event baseline | S | RISK | Complete: `experiments\h_a2_h_l1_post_proxy_decision.json` and `docs\H_A2_H_L1_POST_PROXY_DECISION.md` select `pre_register_h_a2_residual_adverse_day_analysis` as the next safe local/no-paid path. The decision keeps H-A2 active at E1, keeps H-L1 live LLM/news research blocked until real timestamp-clean news cases exist, and forbids network, paid data, broker requests, live LLM calls, GDELT live retry during cooldown, paper trading, operational validation, real-money trading, and E2 claims. `python scripts\validate_h_a2_h_l1_post_proxy_decision.py` and `python -m unittest tests.test_validate_h_a2_h_l1_post_proxy_decision` pass |
| H-A2.21 | Pre-register H-A2 residual/adverse-day analysis before running it | S | RISK | Complete: `experiments\h_a2_residual_adverse_day_analysis_preregistration.json` and `docs\H_A2_RESIDUAL_ADVERSE_DAY_ANALYSIS_PREREGISTRATION.md` define the local-only analysis of non-risk losing days, macro-only losing days, residual adverse buckets, chronological split, MinTRL/PSR/DSR/search-log handling, big-day context, and decision rules for revising, parking, or prioritizing exact replay. `python scripts\validate_h_a2_residual_adverse_day_preregistration.py` and `python -m unittest tests.test_validate_h_a2_residual_adverse_day_preregistration` pass. No network, paid data, broker request, IBKR request, GDELT live retry, LLM call, paper trading, operational validation, real-money trading, or E2 claim is approved |
| H-A2.22 | Run the pre-registered H-A2 residual/adverse-day analysis using local artifacts only | M | RISK | Complete: `scripts\run_h_a2_residual_adverse_day_analysis.py` generated `reports\diagnostics\h_a2_residual_adverse_day_analysis.json`, `.md`, and `reports\diagnostics\search_logs\h_a2_residual_adverse_day_analysis_search_log.jsonl`. Result is E1 `ยังสรุปไม่ได้`: 90 trade days, 64 non-risk trade days, 40 non-risk losing days, 26 macro-only trade days, 21 macro-only losing days, non-risk loss rate `0.625`, macro-only loss rate `0.807692`, and negative 5-minute followthrough share among non-risk losses `0.75`. Decision is `revise_h_a2_before_exact_replay`, so next safe action is to pre-register a revised H-A2 condition with opening-followthrough failure-mode checks before exact replay, paid data, IBKR request, LLM call, or paper trading. `python scripts\validate_h_a2_residual_adverse_day_analysis.py`, `python -m unittest tests.test_h_a2_residual_adverse_day_analysis`, and research log `026-higanbana-h-a2-residual-adverse-day-analysis.md` pass/pushed |
| H-A2.23 | Pre-register the revised H-A2 opening-followthrough condition before any local diagnostic run | S | RISK | Complete: `experiments\h_a2_revised_opening_followthrough_condition_preregistration.json` and `docs\H_A2_REVISED_OPENING_FOLLOWTHROUGH_CONDITION_PREREGISTRATION.md` restate H-A2 as clean macro/VIX regime plus opening-followthrough confirmation, using H-A2.22 only to define the mechanism family. The preregistration requires train-only threshold locking, chronological OOS holdout, residual-loss reduction check, mechanism consistency check, MinTRL/PSR, DSR/search log, big-day context, implementable PnL/cost drag, and no OOS tuning. `python scripts\validate_h_a2_revised_opening_followthrough_preregistration.py` and `python -m unittest tests.test_validate_h_a2_revised_opening_followthrough_preregistration` pass. No network, paid data, IBKR request, GDELT retry, LLM call, paper trading, operational validation, real-money trading, or E2 claim is approved |
| H-A2.24 | Run the revised H-A2 opening-followthrough condition diagnostic using local artifacts only | M | RISK | Complete: `scripts\run_h_a2_revised_opening_followthrough_condition.py` generated `reports\experiments\h_a2_revised_opening_followthrough_condition_summary.json`, `.md`, and search log. Result is E1 `ยังสรุปไม่ได้`: train-only threshold `0.001` was locked before OOS evaluation; baseline OOS non-risk trades were 34 with loss rate `0.617647`, revised OOS keeps 13 trades with loss rate `0.076923`, and OOS loss count drops from 21 to 1. This is still under-sampled and not exact 2022-10 ORB replay, so no paid data, IBKR request, LLM call, paper trading, operational validation, real-money trading, or E2 claim is approved. Research log `027-higanbana-h-a2-revised-opening-followthrough-condition.md` was written |
| H-A2.25 | Pre-register robustness audit for the locked H-A2.24 revised condition before any further local run | S | RISK | Complete: `experiments\h_a2_revised_condition_robustness_preregistration.json`, `docs\H_A2_REVISED_CONDITION_ROBUSTNESS_PREREGISTRATION.md`, `scripts\validate_h_a2_revised_condition_robustness_preregistration.py`, and `tests\test_validate_h_a2_revised_condition_robustness_preregistration.py` lock threshold `0.001` and define threshold-provenance, big-day dependency, concentration, skip-cost, and sample-adequacy checks. This is E0 only and forbids threshold search, OOS tuning, exact replay, paid data, IBKR request, GDELT retry, LLM call, paper trading, operational validation, real-money trading, and E2 claims |
| H-A2.26 | Run the locked H-A2.25 robustness audit using local artifacts only | M | RISK | Complete: `scripts\run_h_a2_revised_condition_robustness.py` generated `reports\experiments\h_a2_revised_condition_robustness_summary.json`, `.md`, and search log. Result is E1 `ยังสรุปไม่ได้`: threshold `0.001` stayed locked, no threshold search or OOS tuning was used, baseline OOS non-risk trades were 34, retained OOS trades were 13, skipped OOS trades were 21, retained minus skipped average implementable PnL was `106.556776`, and the result survives basic big-day/concentration checks but remains under-sampled/underpowered. No paid data, IBKR request, GDELT retry, LLM call, exact replay, paper trading, operational validation, real-money trading, or E2 claim is approved |
| H-A2.27 | Pre-register locked-condition signal-attribution audit before treating threshold `0.001` as a tradable rule | S | RISK | Complete: `experiments\h_a2_locked_condition_signal_attribution_preregistration.json`, `docs\H_A2_LOCKED_CONDITION_SIGNAL_ATTRIBUTION_PREREGISTRATION.md`, `scripts\validate_h_a2_locked_condition_signal_attribution_preregistration.py`, and tests define the no-paid local audit that must classify the locked condition as deployable entry filter, delayed-entry candidate, or diagnostic proxy only. It forbids threshold changes, new OOS-selected filters, exact replay, paid data, IBKR request, GDELT retry, LLM call, paper trading, operational validation, real-money trading, and E2 claims |
| H-A2.28 | Run locked-condition signal-attribution audit using local artifacts only | M | RISK | Complete: `scripts\run_h_a2_locked_condition_signal_attribution.py` generated `reports\experiments\h_a2_locked_condition_signal_attribution_summary.json`, `.md`, and search log. Result is E1 `ยังสรุปไม่ได้`: threshold `0.001` stayed locked and no new threshold/filter search was used, but the full condition includes a 15-minute conflict component known around 09:45 ET, after the original 09:35 ET entry. Treat it as delayed-entry candidate and diagnostic proxy only; no exact replay, paid data, IBKR request, LLM call, paper trading, operational validation, real-money trading, or E2 claim is allowed |
| H-A2.29 | Pre-register delayed-entry H-A2 condition after the 15-minute conflict window | S | RISK | Complete: `experiments\h_a2_delayed_entry_condition_preregistration.json`, `docs\H_A2_DELAYED_ENTRY_CONDITION_PREREGISTRATION.md`, `scripts\validate_h_a2_delayed_entry_condition_preregistration.py`, and tests define the next local no-paid diagnostic. Candidate decision time is `09:45:00` ET, threshold `0.001` remains locked, original 09:35 PnL must not be reused as delayed-entry PnL, and the future run must report delayed-entry fill/cost handling, train/OOS counts, sample labels, and E1 cap |
| H-A2.30 | Run delayed-entry H-A2 diagnostic using local artifacts only | M | RISK | Complete: `scripts\run_h_a2_delayed_entry_condition.py` generated `reports\experiments\h_a2_delayed_entry_condition_summary.json`, `.md`, and search log. Result is E1 `ยังสรุปไม่ได้`: the locked condition is timestamp-clean at `09:45:00` ET, but source artifacts do not contain auditable 09:45 delayed-entry option quotes/fills, so delayed-entry implementable PnL is not computable and original 09:35 PnL was not reused. Research log `030-higanbana-h-a2-delayed-entry-condition.md` was pushed |
| H-A2.31 | Pre-register timestamp-clean original-entry H-A2 revision using only 09:35-known features | S | RISK | Complete: `experiments\h_a2_original_entry_revision_preregistration.json`, `docs\H_A2_ORIGINAL_ENTRY_REVISION_PREREGISTRATION.md`, `scripts\validate_h_a2_original_entry_revision_preregistration.py`, and tests define the next local no-paid diagnostic branch. Candidate decision time remains `09:35:00` ET, threshold `0.001` stays locked, the 15-minute conflict component is forbidden, no new OOS-selected filter is allowed, and future output is capped at E1 |
| H-A2.32 | Run timestamp-clean original-entry H-A2 revision diagnostic using local artifacts only | M | RISK | Complete: `scripts\run_h_a2_original_entry_revision.py` generated `reports\experiments\h_a2_original_entry_revision_summary.json`, `.md`, and search log. Result is E1 `ยังสรุปไม่ได้`: the 09:35-only rule uses only clean macro/VIX and `proxy_5m_followthrough >= 0.001`, excludes the 15-minute conflict component, keeps threshold `0.001` locked, retains 14 OOS trade days, and remains under-sampled/underpowered. Research log `031-higanbana-h-a2-original-entry-revision.md` was pushed |
| H-A2.33 | Pre-register stricter original-entry robustness/prioritization review | S | RISK | Complete: `experiments\h_a2_original_entry_robustness_prioritization_preregistration.json`, `docs\H_A2_ORIGINAL_ENTRY_ROBUSTNESS_PRIORITIZATION_PREREGISTRATION.md`, `scripts\validate_h_a2_original_entry_robustness_prioritization_preregistration.py`, and tests define the next local no-paid review. It preserves the H-A2.32 09:35-only rule and threshold `0.001`, forbids threshold search/OOS tuning/new filters, and requires leave-one/big-day dependency, regime/calendar concentration, skip-cost tradeoff, and validation-priority decision checks. No research log is required because this is E0 preregistration |
| H-A2.34 | Run stricter original-entry robustness/prioritization review | M | RISK | Complete: `scripts\run_h_a2_original_entry_robustness_prioritization.py` generated `reports\experiments\h_a2_original_entry_robustness_prioritization_summary.json`, `.md`, and search log. Result is E1 `ยังสรุปไม่ได้`: leave-one-out min avg PnL remains positive at `63.209231`, largest retained day share is `0.151153`, retained minus skipped avg PnL is `106.0`, but retained OOS is only 14 trade days and has no high-VIX retained bucket. Research log `032-higanbana-h-a2-original-entry-robustness-prioritization.md` was pushed |
| H-A2.35 | Pre-register independent validation-data feasibility plan | S | RISK | Complete: `experiments\h_a2_independent_validation_feasibility_preregistration.json`, `docs\H_A2_INDEPENDENT_VALIDATION_FEASIBILITY_PREREGISTRATION.md`, `scripts\validate_h_a2_independent_validation_feasibility_preregistration.py`, and tests define the next no-paid H-A2 planning step. It preserves the locked `09:35:00` signal and threshold `0.001`, requires gap inventory, validation target definition, no-paid source inventory, paid decision tree, and next-action selection, and forbids paid data, live cost estimates, IBKR requests, exact replay, LLM calls, GDELT retries, paper trading, and E2 claims |
| H-A2.36 | Run independent validation-data feasibility diagnostic | M | RISK | Complete: `scripts\run_h_a2_independent_validation_feasibility.py` generated `reports\diagnostics\h_a2_independent_validation_feasibility.json`, `.md`, and search log. Result is E1 `ยังสรุปไม่ได้`: no-paid sources can define validation gaps and regime requirements, but cannot add independent implementable SPY 0DTE PnL days. Retained OOS remains 14 trade days and high-VIX retained coverage is still missing. Under the post-H-A2.40 cost guard, the rerunnable artifact now selects `pause_paid_path_run_no_paid_gap_report_or_wait_for_topup`; earlier H-A2.37-H-A2.40 cost/download artifacts remain the completed one-day path. Research log `033-higanbana-h-a2-independent-validation-feasibility.md` was pushed |
| H-A2.37 | Pre-register narrow independent-validation paid-cost estimate plan | S | RISK | Complete: `experiments\h_a2_independent_validation_paid_cost_plan_preregistration.json`, `docs\H_A2_INDEPENDENT_VALIDATION_PAID_COST_PLAN_PREREGISTRATION.md`, `scripts\validate_h_a2_independent_validation_paid_cost_plan_preregistration.py`, and tests define the next estimate-only H-A2 path. The plan names exact windows (`2025-04-08`, high-VIX pack `2025-04-04` to `2025-04-11`, and control packs `2025-02-03` to `2025-02-14` / `2025-05-05` to `2025-05-16`), required OPRA/SPY/regime fields, current `$5.010294` headroom, and forbids live estimates, paid downloads, IBKR requests, exact replay, paper trading, and E2 claims from the preregistration itself |
| H-A2.38 | Run one-day metadata-only independent-validation cost estimate | S | RISK | Complete: `scripts\estimate_h_a2_independent_validation_cost.py` generated `reports\data_cost\h_a2_independent_validation_paid_cost_plan.json`, `.md`, `reports\data_cost\h_a2_independent_validation_paid_cost_estimate.json`, and `.md` for `sample_cost_probe_high_vix_one_day` (`2025-04-08`). Live metadata estimate is `$0.504662` for 15 requests, no download was performed, projected usage would be `$120.494368` under the `$125` guard, and `download_allowed_under_current_guard=false` because a separate download decision artifact is still required. `scripts\audit_paid_costs.py` now tracks this as estimated-only, not committed cost |
| H-A2.39 | Create the one-day paid download decision artifact before pulling data | S | RISK | Complete: `experiments\h_a2_independent_validation_paid_download_decision.json`, `docs\H_A2_INDEPENDENT_VALIDATION_PAID_DOWNLOAD_DECISION.md`, `scripts\validate_h_a2_independent_validation_paid_download_decision.py`, and tests approve only the 15-request `2025-04-08` high-VIX sample after paid-cost audit pass. The decision preserves the locked 09:35-only signal and threshold `0.001`, forbids broad 2025 data, other validation packs, exact replay, paper trading, and E2 claims |
| H-A2.40 | Execute the controlled one-day independent-validation download | S | RISK | Complete: `scripts\download_h_a2_independent_validation_data.py --execute` wrote `reports\data_cost\databento_download_result_h_a2_independent_validation_2025_04_08.json` and `.md`, downloading/cache-confirming 15 Databento requests and 54,014,593 bytes for `2025-04-08`. `scripts\validate_h_a2_independent_validation_download_result.py`, `scripts\audit_paid_costs.py`, and `scripts\audit_research_readiness.py` pass for the new state. Cost guard now uses `$120.494368`, leaving `$4.505632`; this is data acquisition only and does not approve exact replay, paper trading, operational validation, or E2 |
| H-A2.41 | Pre-register the local import/normalization/diagnostic step for the downloaded one-day sample | S | RISK | Complete: `experiments\h_a2_independent_validation_import_diagnostic_preregistration.json`, `docs\H_A2_INDEPENDENT_VALIDATION_IMPORT_DIAGNOSTIC_PREREGISTRATION.md`, `scripts\validate_h_a2_independent_validation_import_diagnostic_preregistration.py`, and tests define the next local-only diagnostic. It allows only parsing the already-downloaded raw DBN files after validator pass, preserves the locked 09:35-only signal and threshold `0.001`, and requires raw-file inventory, SPY bar import, OPRA quote import, timestamp alignment, signal reconstruction, and entry/exit quote-availability checks before any exact replay or E2 claim |
| H-A2.42 | Run the local import/normalization/diagnostic step for the one-day independent-validation sample | M | RISK | Complete: `scripts\run_h_a2_independent_validation_import_diagnostic.py` generated `reports\diagnostics\h_a2_independent_validation_import_diagnostic.json`, `.md`, and search log. Result is E1 `ยังสรุปไม่ได้`: all 15 raw DBN files parse locally, SPY bars cover 09:30-15:59 ET, OPRA windows contain 1,686,591 quote rows and 43,965 0DTE valid-mid rows, and the locked 09:35 5-minute put followthrough passes threshold `0.001`; however clean macro/VIX is false because prior VIX was `46.98`, so no candidate trade signal exists. No exact replay, PnL, paper trading, operational validation, real-money trading, or E2 claim is approved. Research log `034-higanbana-h-a2-independent-validation-import-diagnostic.md` was pushed |
| H-A2.43 | Pre-register the normal/control independent-validation sample decision after the high-VIX availability sample | S | RISK | Complete: `experiments\h_a2_normal_control_sample_decision.json`, `docs\H_A2_NORMAL_CONTROL_SAMPLE_DECISION.md`, `scripts\validate_h_a2_normal_control_sample_decision.py`, and tests pause the prior high-VIX-first sequence and select `low_normal_vix_control_pack` (`2025-02-03` to `2025-02-14`) as the next metadata-only estimate target. The decision logs selected key env `DATABENTO_API_MO` without storing key value, preserves the locked 09:35-only rule and threshold `0.001`, and forbids paid download, exact replay, paper trading, and E2 claims |
| H-A2.44 | Run the normal/control metadata-only cost estimate for `low_normal_vix_control_pack` | S | RISK | Complete: `scripts\estimate_h_a2_independent_validation_cost.py --live --group-live-requests` generated `reports\data_cost\h_a2_normal_control_low_normal_vix_control_pack_cost_estimate.json` and `.md`. Result is E0 cost-control evidence only: 10 normal/control local-VIX trading days, 150 planned required windows, 20 grouped conservative Databento metadata calls, estimated cost `$5.398913`, selected key env `DATABENTO_API_MO`, projected selected-key usage `$5.398913` under the `$100` per-key cap and `$200` MO/AI pool cap, no download performed, and separate download decision still required |
| H-A2.45 | Create the normal/control paid download decision artifact before pulling data | S | RISK | Complete: `experiments\h_a2_normal_control_paid_download_decision.json`, `docs\H_A2_NORMAL_CONTROL_PAID_DOWNLOAD_DECISION.md`, `scripts\validate_h_a2_normal_control_paid_download_decision.py`, and tests approve only `low_normal_vix_control_pack` after paid-cost audit pass. The decision preserves 10 dates from `2025-02-03` to `2025-02-14`, selected key env `DATABENTO_API_MO`, the locked 09:35-only signal and threshold `0.001`, and forbids broad 2025 buying, `post_stress_normalization_control_pack`, high-VIX-first resumption, exact replay, paper trading, and E2 claims |
| H-A2.46 | Execute the controlled normal/control download | S | RISK | Complete: `scripts\download_h_a2_normal_control_data.py --execute` generated `reports\data_cost\databento_download_result_h_a2_normal_control_low_normal_vix_control_pack.json` and `.md`, downloading 20 grouped files for 10 dates, totaling 741,157,996 bytes at logged estimate `$5.398913` using selected key env `DATABENTO_API_MO`. `scripts\validate_h_a2_normal_control_download_result.py` passes. This is E0 data acquisition only; it does not approve exact replay, strategy PnL, paper trading, operational validation, real-money trading, or E2 |
| H-A2.47 | Pre-register the local import/normalization/diagnostic step for the normal/control pack | S | RISK | Complete: `experiments\h_a2_normal_control_import_diagnostic_preregistration.json`, `docs\H_A2_NORMAL_CONTROL_IMPORT_DIAGNOSTIC_PREREGISTRATION.md`, `scripts\validate_h_a2_normal_control_import_diagnostic_preregistration.py`, and tests define the next local-only diagnostic. It allows only parsing the already-downloaded normal/control raw DBN files after validator pass, preserves the locked 09:35-only signal and threshold `0.001`, and requires raw-file inventory, SPY bar import, OPRA quote import, timestamp alignment, signal reconstruction, and entry/exit quote-availability checks before any exact replay or E2 claim |
| H-A2.48 | Run the local normal/control import/normalization/diagnostic step | M | RISK | Complete: `scripts\run_h_a2_normal_control_import_diagnostic.py` generated `reports\diagnostics\h_a2_normal_control_import_diagnostic.json`, `.md`, `reports\diagnostics\search_logs\h_a2_normal_control_import_diagnostic_search_log.jsonl`, and normalized inventory outputs. Result is E1 `ยังสรุปไม่ได้`: 20 downloaded/grouped DBN files for 10 dates parse locally, SPY bars contain 3,900 rows, OPRA quote windows contain 35,284,142 rows, 0DTE valid-mid quote rows total 557,254, timestamp alignment passes for 10/10 dates, and the locked 09:35 threshold `0.001` signal finds exactly one candidate-ready date, `2025-02-11`, with 0 data-blocked candidate dates. No exact replay, PnL, paper trading, operational validation, real-money trading, or E2 claim is approved. Research log `035-higanbana-h-a2-normal-control-import-diagnostic.md` was written and pushed |
| H-A2.49 | Pre-register bounded exact replay for the single normal/control candidate date | S | RISK | Complete: `experiments\h_a2_normal_control_exact_replay_preregistration.json`, `docs\H_A2_NORMAL_CONTROL_EXACT_REPLAY_PREREGISTRATION.md`, `scripts\validate_h_a2_normal_control_exact_replay_preregistration.py`, and tests define the next local-only replay diagnostic. Scope is only `2025-02-11`, direction `call`, locked 09:35 threshold `0.001`, 15:45 forced close, nearest discrete strike rounding, separate `mid_pnl` and `implementable_pnl`, and `$0.64` per-leg fees. The future run may compute candidate-date PnL only after this validator passes, remains E1 diagnostic unless separately upgraded, and forbids new downloads, non-candidate replay, threshold changes, new OOS-selected filters, IBKR requests, LLM calls, GDELT retries, paper trading, operational validation, real-money trading, and E2 claims |
| H-A2.49a | Run bounded exact replay for the single normal/control candidate date | M | RISK | Complete: `scripts\run_h_a2_normal_control_exact_replay.py` generated `reports\diagnostics\h_a2_normal_control_exact_replay.json`, `.md`, and `reports\diagnostics\search_logs\h_a2_normal_control_exact_replay_search_log.jsonl`. Result is E1 `ยังสรุปไม่ได้`: selected call vertical `605/607`, `mid_pnl=-22.00`, `implementable_pnl=-26.56`, cost drag `$4.56`, sample count `1`, under-sampled/underpowered. Research log `036-higanbana-h-a2-normal-control-exact-replay.md` was written and pushed |
| H-A2.50 | Pre-register the next H-A2 validation-data or sample-expansion decision after H-A2.49 | S | RISK | Complete: `experiments\h_a2_post_exact_replay_sample_expansion_decision.json`, `docs\H_A2_POST_EXACT_REPLAY_SAMPLE_EXPANSION_DECISION.md`, `scripts\validate_h_a2_post_exact_replay_sample_expansion_decision.py`, and tests select `post_stress_normalization_control_pack` as the next bounded validation-data path using `DATABENTO_API_AI`, while preserving threshold `0.001` and forbidding exact replay/PnL/E2/paper claims |
| H-A2.51 | Run metadata-only Databento cost estimate for `post_stress_normalization_control_pack` | S | RISK | Complete: `reports\data_cost\h_a2_post_stress_normalization_control_pack_cost_estimate.json` and `.md` estimate 10 dates, 150 planned windows grouped into 20 metadata calls, selected key env `DATABENTO_API_AI`, estimated cost `$5.558642`, no download performed |
| H-A2.52 | Create paid download decision for `post_stress_normalization_control_pack` | S | RISK | Complete: `experiments\h_a2_post_stress_normalization_control_paid_download_decision.json`, `scripts\validate_h_a2_post_stress_normalization_control_paid_download_decision.py`, and tests approve only the 2025-05-05 to 2025-05-16 pack after paid-cost audit pass; no threshold change, broader dates, exact replay, PnL, E2, or paper-trading claim is allowed |
| H-A2.53 | Download only the approved `post_stress_normalization_control_pack` | M | RISK | Complete: `reports\data_cost\databento_download_result_h_a2_post_stress_normalization_control_pack.json` and `.md` report 20/20 grouped files downloaded, total bytes `1,057,077,119`, selected key env `DATABENTO_API_AI`, estimated cost `$5.558642`, and status `pass`; this is E0 data acquisition only |
| H-A2.54 | Pre-register import/normalization/diagnostic for the downloaded post-stress normalization/control pack | S | RISK | Complete: `experiments\h_a2_post_stress_normalization_control_import_diagnostic_preregistration.json`, `docs\H_A2_POST_STRESS_NORMALIZATION_CONTROL_IMPORT_DIAGNOSTIC_PREREGISTRATION.md`, `scripts\validate_h_a2_post_stress_normalization_control_import_diagnostic_preregistration.py`, and tests define the next local-only diagnostic. It allows only parsing the already-downloaded post-stress normalization/control raw DBN files after validator pass, preserves the locked 09:35-only signal and threshold `0.001`, requires raw-file inventory, SPY bar import, OPRA quote import, timestamp alignment, signal reconstruction, candidate availability, and explicit handling of the 2025-05-06 reduced-quality note before any exact replay or E2 claim |
| H-A2.55 | Run the local post-stress normalization/control import/normalization/diagnostic step | M | RISK | Complete: `scripts\run_h_a2_post_stress_normalization_control_import_diagnostic.py` generated `reports\diagnostics\h_a2_post_stress_normalization_control_import_diagnostic.json`, `.md`, `reports\diagnostics\search_logs\h_a2_post_stress_normalization_control_import_diagnostic_search_log.jsonl`, and normalized inventory outputs using only already-downloaded DBN files. Result is E1 `ยังสรุปไม่ได้`: 20 raw files parse locally, SPY bars contain 3,900 rows, OPRA quote windows contain 36,296,592 rows, 0DTE valid-mid quote rows total 939,097, timestamp alignment passes, and the locked 09:35 threshold `0.001` signal finds exactly one candidate-ready date, `2025-05-05`. No exact replay, PnL, paper trading, operational validation, real-money trading, or E2 claim is approved. Research log `037-higanbana-h-a2-post-stress-normalization-control-import-diagnostic.md` was written |
| H-A2.56 | Pre-register bounded exact replay for the post-stress candidate-ready date | S | RISK | Complete: `experiments\h_a2_post_stress_normalization_control_exact_replay_preregistration.json`, `docs\H_A2_POST_STRESS_NORMALIZATION_CONTROL_EXACT_REPLAY_PREREGISTRATION.md`, `scripts\validate_h_a2_post_stress_normalization_control_exact_replay_preregistration.py`, and tests define the next local-only replay diagnostic. Scope is only `2025-05-05`, direction `call`, locked 09:35 threshold `0.001`, 15:45 forced close, nearest discrete strike rounding, separate `mid_pnl` and `implementable_pnl`, and `$0.64` per-leg fees. The future run may compute candidate-date PnL only after this validator passes, remains E1 diagnostic unless separately upgraded, and forbids new downloads, non-candidate replay, threshold changes, new OOS-selected filters, IBKR requests, LLM calls, GDELT retries, paper trading, operational validation, real-money trading, and E2 claims |
| H-A2.57 | Run bounded exact replay for the post-stress candidate-ready date | M | RISK | Complete: `scripts\run_h_a2_post_stress_normalization_control_exact_replay.py` generated `reports\diagnostics\h_a2_post_stress_normalization_control_exact_replay.json`, `.md`, and `reports\diagnostics\search_logs\h_a2_post_stress_normalization_control_exact_replay_search_log.jsonl` using only the pre-registered `2025-05-05` call candidate. Result is E1 `ยังสรุปไม่ได้`: selected call vertical `565/567`, `mid_pnl=-28.00`, `implementable_pnl=-32.56`, cost drag `$4.56`, sample count `1`, under-sampled/underpowered. Research log `038-higanbana-h-a2-post-stress-normalization-control-exact-replay.md` was written |
| H-A2.58 | Decide H-A2 after two exact-replayed candidates | S | RISK | Complete: `experiments\h_a2_post_two_exact_replay_decision.json`, `docs\H_A2_POST_TWO_EXACT_REPLAY_DECISION.md`, `scripts\validate_h_a2_post_two_exact_replay_decision.py`, and tests combine the `2025-02-11` and `2025-05-05` exact replays. The two candidates total `mid_pnl=-50.00`, `implementable_pnl=-59.12`, and cost drag `$9.12`; both candidates are negative after implementable costs and still under-sampled/underpowered. Decision: `revise_h_a2_mechanism_before_more_sample_expansion`. No data purchase, exact replay expansion, threshold search, OOS-selected filter, E2 claim, paper trading, operational validation, or real-money claim is allowed from this decision |
| H-A2.59 | Pre-register revised H-A2 market mechanism before more data expansion | S | RISK | Complete: `experiments\h_a2_mechanism_revision_preregistration.json`, `docs\H_A2_MECHANISM_REVISION_PREREGISTRATION.md`, `scripts\validate_h_a2_mechanism_revision_preregistration.py`, and tests reframe H-A2 as a conditional continuation hypothesis. It states that direction alone is not enough; post-entry magnitude, implementable cost drag, and regime context must explain when ORB debit spreads can beat friction. It forbids paid data, exact replay expansion, threshold search, OOS-selected filters, paper trading, operational validation, real-money trading, and E2 claims |
| H-A2.60 | Run local/no-paid H-A2 mechanism-revision audit | M | RISK | Complete: `scripts\run_h_a2_mechanism_revision_audit.py` generated `reports\diagnostics\h_a2_mechanism_revision_audit.json`, `.md`, and `reports\diagnostics\search_logs\h_a2_mechanism_revision_audit_search_log.jsonl`. Result is E1 `ยังสรุปไม่ได้`: both exact-replayed candidates were directionally correct at the underlying level, but both option spreads lost after implementable costs; total `mid_pnl=-50.00`, total `implementable_pnl=-59.12`, cost drag `$9.12`, and forced-close price did not reach the long strike in 2 of 2 cases. Decision: `preregister_train_only_revised_rule`. Research log `039-higanbana-h-a2-mechanism-revision-audit.md` was written |
| H-A2.61 | Pre-register breakeven-aware train-only H-A2 revised rule | S | RISK | Complete: `experiments\h_a2_breakeven_aware_rule_preregistration.json`, `docs\H_A2_BREAKEVEN_AWARE_RULE_PREREGISTRATION.md`, `scripts\validate_h_a2_breakeven_aware_rule_preregistration.py`, and tests define the revised H-A2 rule family as E0 control evidence. It separates payoff-feasibility targets from decision-time inputs: future post-entry movement, forced-close option quotes, same-day realized PnL, and OOS-selected filters are forbidden as live inputs. No paid data, exact replay expansion, threshold/filter change, E2 claim, paper trading, operational validation, or real-money work is approved. No research log was written because this is preregistration, not a completed experiment |
| H-A2.62 | Run breakeven-aware train-only H-A2 diagnostic | M | RISK | Complete: `scripts\run_h_a2_breakeven_aware_rule_train_diagnostic.py` generated `reports\diagnostics\h_a2_breakeven_aware_rule_train_diagnostic.json`, `.md`, and search log. Result is E1 `ยังสรุปไม่ได้`: current train artifacts can run surrogate followthrough trials, but cannot lock a true breakeven-aware option rule because train rows lack per-candidate entry strike mapping, entry implementable debit, bid/ask width, liquidity, and train-distribution strike-reachability targets. Decision is `write_targeted_data_regime_expansion_plan`; research log `040-higanbana-h-a2-breakeven-aware-train-diagnostic.md` was written and pushed |
| H-A2.63 | Pre-register targeted data/regime expansion plan for breakeven-aware ORB evidence | M | RISK | Complete: `experiments\h_a2_targeted_data_regime_expansion_plan.json`, `docs\H_A2_TARGETED_DATA_REGIME_EXPANSION_PLAN.md`, `scripts\validate_h_a2_targeted_data_regime_expansion_plan.py`, and tests define the targeted data/regime expansion plan as E0 control evidence. The plan prioritizes `train_candidate_geometry_backfill`, then normal/control geometry, stress-regime geometry, and future OOS locked-rule evaluation. It names required fields/windows for entry strike mapping, entry implementable debit, bid/ask width, liquidity, forced-close value, regime labels, and MinTRL/PSR coverage. No paid download, OOS rule evaluation, paper trading, operational validation, real-money trading, or E2 claim is approved |
| H-A2.64 | Run cache inventory and metadata/cost-estimate planning for H-A2.63 target sets | M | RISK | Complete: `scripts\run_h_a2_targeted_geometry_cache_inventory.py` generated `reports\data_cost\h_a2_targeted_geometry_cache_inventory_and_cost_plan.json` and `.md`; `scripts\validate_h_a2_targeted_geometry_cache_inventory.py` and tests pass. Result is E0 `complete_no_download_cost_estimate_deferred`: train cache is ready for 30/30 geometry dates, normal/control has 2 candidate-ready dates from existing downloads, stress geometry has local option quotes for 13 dates but remains blocked by missing real 2022 SPY bars. No network, live metadata call, paid download, threshold selection, OOS rule evaluation, paper trading, operational validation, real-money trading, or E2 claim was made |
| H-A2.65 | Acquire and integrity-register the approved 13-date October 2022 SPY bar block | M | RISK | Complete: `reports\data_cost\h_a2_2022_ibkr_spy_bars_download_result.json` records 13/13 dates, 5,070 rows, raw SHA-256 `d95cac46...1589de8`, canonical SHA-256 `7f79e86a...521c771`, read-only broker access, and zero orders. `scripts\verify_data_integrity.py` supports and verifies the IBKR raw/canonical pair |
| H-A2.66 | Run the preregistered October 2022 exact stress replay and enforce the density checkpoint | M | RISK | Complete: `reports\experiments\h_a2_2022_10_stress_exact_replay_summary.json` reports E1 `ยังสรุปไม่ได้`: all 13 dates pass bar coverage, 3 show a raw 09:35 breakout, but all 13 fail the ex-ante clean macro/VIX gate and therefore produce 0 candidate trades and no PnL. The minimum-2-trade checkpoint fails, September purchase is forbidden, and no E2, paper-trading, operational-validation, or real-money claim is allowed |
| H-A2.6 | Write research log `011-higanbana-...` only if a real experiment or formal deferred/falsification result completes | S | OK | Complete for H-A2.2: `research_log\011-higanbana-macro-conditioned-orb-reanalysis.md` was written and pushed; `python scripts\audit_research_logs.py` passes |

**Track complete when**: H-A2 is either falsified cheaply, remains active with a justified next data/source target, or earns a clean path toward E2 using untouched validation data. Missing exact-resolution data must not stop lower-resolution falsification or prioritization work when the allowed claim is explicitly downgraded.

---

## Track H-B2: Sub-System B At Realistic Scale
**Goal**: Decide whether the put-ratio-with-wing structure has merit at simulated `$10k`/`$25k` scale and under which volatility/skew/regime suitability conditions, without confusing this with tradability on the current `$1k` account or assuming the structure should work every day.
**Dependencies**: Track P0

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| H-B2.1 | Register H-B1 as falsified for `$300` allocation / `$20` risk and H-B2 as simulated-scale structure test | S | RISK | Complete: registry validator passes and H-B1/H-B2 entries exist |
| H-B2.2 | Pre-register account sizes, wing grid, sizing rule, selection rule, and DSR/search-log handling | M | RISK | Complete: `experiments\h_b2_subsystem_b_scale_preregistration.json` validates with fixed `$10k`/`$25k`, `$5/$10/$15/$20` wing grid, 5% account-risk eligibility, no fractional contracts, and 8-trial DSR/search-log handling |
| H-B2.3 | Rerun Sub-System B feasibility using already-cached data only | L | RISK | Complete: `reports\experiments\h_b2_subsystem_b_scale_summary.json`, `.md`, and search log report 8 pre-registered scenarios with implementable PnL, ES95, drawdown, cost drag, sample labels, and no new paid data |
| H-B2.4 | Decide falsified/active/parked based on pre-registered criteria | S | RISK | Complete: H-B2 is parked pending MinTRL_falsify review; zero scenarios had both positive total and OOS implementable PnL, and no deployment/paper-trading edge claim is allowed |
| H-B2.5 | Review whether MinTRL/PSR evidence reopens or closes the current H-B2 grid | S | RISK | Complete: `reports\experiments\h_b2_falsification_review.json`, `.md`, `scripts\validate_h_b2_falsification_review.py`, and `tests\test_h_b2_falsification_review.py` confirm E1 `ไม่ผ่าน` for the current grid: 0 scenarios had positive total plus OOS implementable PnL, all scenarios remain underpowered/under-sampled, the current grid is not resurrected, and Sub-System B is not deleted from scope |

| H-B2.6 | Before reopening Sub-System B, write a mechanism note that reframes put-ratio-with-wing as a regime/suitability-conditioned structure instead of an always-on strategy | S | RISK | New user decision. Verification: future artifact must define the exact question, proposed volatility/skew/regime filters, falsification evidence, and no OOS-fit rule before any rerun |

**Track complete when**: Complete for the current v2 diagnostic and falsification-review cycle. H-B2 current grid remains parked and not resurrected. Sub-System B is not deleted, but no new H-B2 run should happen until a regime/suitability-conditioned mechanism note is pre-registered.

---

## Track H-G1: Gamma/OI Proxy Validity
**Goal**: Validate or kill the signed-OI gamma proxy as a data signal before any NOVI/net-gamma strategy claim.
**Dependencies**: Track P0 and existing OPRA OI/enrichment probes

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| H-G1.1 | Issue `docs\GAMMA_AGGREGATION_VALIDATION_POLICY.md` v2 before rerun | M | RISK | Complete: v1 fail remains preserved; v2 requires dual raw-row and bucket-weighted coverage reporting |
| H-G1.2 | Pre-register a 12-date regime set across low/normal/high volatility, macro/no-macro, trend, train, and OOS buckets | M | RISK | Complete: `python scripts\validate_h_g1_regime_date_set.py` passes with 12 dates, low/normal/high counts 4/4/4, train/OOS 6/6, macro/no-macro 6/6 |
| H-G1.3 | Estimate and buy only the missing OPRA statistics/OI days if cost guard passes | M | RISK | Complete: live metadata estimate for 11 missing dates was `$4.082227`, download gate passed, 11 OPRA `statistics` DBN files were downloaded with checksums, and post-download `audit_paid_costs.py` reports guard usage `$109.082227` with `$15.917773` headroom |
| H-G1.4 | Rerun enrichment and gamma diagnostic under v2 | L | RISK | Complete: `reports\diagnostics\h_g1_gamma_regime_diagnostic_summary.json` reports E1 `ยังสรุปไม่ได้`; stability, timestamp discipline, economic sign, and search-log gates pass, but coverage gate fails |
| H-G1.5 | Register result as active, falsified, or still blocked | S | RISK | Complete: H-G1 is `active_blocked`; no NOVI/net-gamma strategy use is allowed because raw-row and required-bucket coverage failed |
| H-G1.6 | Review H-G1 data-quality blockers before any replacement manifest or paid data | M | RISK | Complete: `reports\diagnostics\h_g1_data_quality_review.json` shows 2 manifest/cache mismatches (`2023-03-13`, `2023-03-22`) with missing SPY bars; excluding those dates raises computed Greeks rate from `0.622771` to `0.739238` and underlying join rate to `1.0`, but 5 required-bucket failures remain |
| H-G1.7 | Create H-G1 manifest v2 repair diagnostic after March SPY bars prove unavailable | M | RISK | Complete: Databento `EQUS.MINI` rejected `2023-03-13`/`2023-03-22` SPY bar repair as `data_start_before_available_start`; `experiments\h_g1_gamma_regime_date_set_preregistration_v2.json` validates with 10 usable dates; `reports\diagnostics\h_g1_gamma_regime_diagnostic_summary_v2_10date.json` shows raw-row coverage passes (`computed_greeks_rate=0.739238`, `underlying_join_rate=1.0`, `open_interest_join_rate=0.989414`) but H-G1 remains blocked by 5 required-bucket computed-Greeks failures |
| H-G1.8 | Diagnose the five required-bucket computed-Greeks failures before any policy change or paid replacement data | S | RISK | Complete: `reports\diagnostics\h_g1_bucket_failure_diagnostic.json` shows all five failures are `blocked_mid_outside_black_scholes_bracket`; four failures retain `>=0.90` computed OI-notional share, but `2023-07-12 otm_put` has only `0.662545`, so H-G1 remains blocked |
| H-G1.9 | Decide whether policy revision alone is enough or manifest v3 is required | S | RISK | Complete: `reports\diagnostics\h_g1_policy_manifest_decision.json` rejects policy revision alone; next is policy v2.1 review artifact plus manifest v3 replacement plan before any new paid OI pull |
| H-G1.10 | Draft policy v2.1 review artifact and manifest v3 replacement plan | M | RISK | Complete: `docs\H_G1_POLICY_V2_1_REVIEW.md` keeps v2.1 review-only, and `experiments\h_g1_gamma_regime_date_set_preregistration_v3_plan.json` validates with one locked in-sample low-volatility high-importance-macro replacement objective for `2023-07-12` before any metadata cost check or paid OI pull |
| H-G1.11 | Create concrete manifest v3 from the locked replacement plan | M | RISK | Complete: `reports\diagnostics\h_g1_manifest_v3_candidate_selection.json` ranks 15 eligible local-cache candidates by locked macro/VIX rules and selects `2023-09-13`; `experiments\h_g1_gamma_regime_date_set_preregistration_v3.json` validates with exactly one `needs_metadata_cost_check` OI day and no paid/network data |
| H-G1.12 | Run metadata cost check for the one v3 replacement OPRA statistics/OI day | S | RISK | Complete: `reports\data_cost\h_g1_gamma_oi_v3_replacement_cost_estimate.json` reports one live metadata request for `2023-09-13`, estimated cost `$0.384999`, projected usage `$109.467226`, decision `pass`, and no download performed |
| H-G1.13 | Download only the approved v3 replacement OPRA statistics/OI day | S | RISK | Complete: `reports\data_cost\h_g1_gamma_oi_download_result_v3_replacement.json` reports exactly one downloaded `2023-09-13_full_utc_day_statistics` DBN file, 2,971,020 bytes, checksum `447d70eee13c9ebff73c09d16546698fb0f634c1f7961bca54bb6eacdb77e357`, and `python scripts\audit_paid_costs.py` remains below `$125` |
| H-G1.14 | Rerun H-G1 enrichment and gamma diagnostic with manifest v3 | L | RISK | Complete: `reports\diagnostics\h_g1_gamma_regime_diagnostic_summary_v3.json` uses manifest v3 plus original H-G1 OI days and the `2023-09-13` replacement OI day. Raw-row coverage, timestamp discipline, stability, economic sign, and search-log gates pass, but bucket-weighted coverage fails on five date/bucket cells |
| H-G1.15 | Diagnose manifest-v3 bucket-weighted coverage failures before any policy change or paid replacement data | M | RISK | Complete: `reports\diagnostics\h_g1_manifest_v3_bucket_failure_diagnostic.json` shows all 55 blocked rows inside the five failed buckets are opposite-right ITM rows caused by moneyness-only bucket definitions; minimum computed OI-notional share is 0.880098, so the blocker is policy-definition, not OI/timestamp/data-availability |
| H-G1.16 | Pre-register a bucket-policy review before any H-G1 policy change or strategy-use claim | M | RISK | Complete: `docs\H_G1_BUCKET_POLICY_REVIEW.md` and `experiments\h_g1_bucket_policy_review_preregistration.json` lock three candidate policies, forbid paid data/new dates/strategy-PnL selection, and validate with `python scripts\validate_h_g1_bucket_policy_review.py` |
| H-G1.17 | Run the no-paid H-G1 bucket-policy comparison diagnostic using manifest-v3 rows only | M | RISK | Complete: `reports\diagnostics\h_g1_bucket_policy_comparison.json` compares current v2 moneyness-only, side-aware required-bucket, and OI/gamma-notional gates on the same manifest-v3 rows. Candidate B `side_aware_required_bucket` passes coverage review across 10/10 dates and is recommended for a separate policy-adoption review, but output remains E1 policy-review-only; policy is not adopted and strategy use is not allowed |
| H-G1.18 | Draft explicit side-aware bucket policy-adoption artifact before any rerun or strategy-use claim | M | RISK | Complete: `docs\H_G1_SIDE_AWARE_BUCKET_POLICY_ADOPTION.md` and `experiments\h_g1_side_aware_bucket_policy_adoption.json` adopt candidate B as `h_g1_required_bucket_policy_v3_side_aware` for the next diagnostic rerun only; strategy use, paid data, new dates, new OI files, new option quotes, and strategy-PnL selection remain forbidden |
| H-G1.19 | Rerun H-G1 gamma diagnostic under the side-aware required-bucket policy using manifest-v3 rows only | L | RISK | Complete: `reports\diagnostics\h_g1_gamma_regime_diagnostic_summary_v3_side_aware.json` reports status `pass_diagnostic_only`, policy id `h_g1_required_bucket_policy_v3_side_aware`, 10 dates, 2,822 quote rows, 2,089 computed Greeks rows, coverage/timestamp/stability/economic-sign/search-log pass, no paid data, no network, no strategy PnL, and `strategy_use_allowed=false` |
| H-G1.20 | Run H-G1 strategy-independent acceptance blocker review before any NOVI/net-gamma strategy filter | M | RISK | Complete: `reports\diagnostics\h_g1_acceptance_blocker_review.json` reports status `blocked_before_strategy_use`, 6 hard blockers, 1 soft blocker, no network, no paid data, no strategy PnL, and `strategy_use_allowed=false` / `paper_trading_allowed=false` |
| H-G1.21 | Pre-register H-G1 gamma strategy-ablation design before any NOVI/net-gamma filter test | M | RISK | Complete: `experiments\h_g1_gamma_strategy_ablation_preregistration.json` and `docs\H_G1_GAMMA_STRATEGY_ABLATION_PREREGISTRATION.md` lock one baseline and three gamma-filtered variants, chronological split, train-only threshold fitting, complete search log/DSR policy, MinTRL/PSR reporting, implementable PnL, big-day dependency, proxy wording, and no OOS tuning |
| H-G1.22 | Implement and run the no-paid H-G1 gamma strategy-ablation runner against the H-G1.21 preregistration | L | RISK | Complete: `reports\experiments\h_g1_gamma_strategy_ablation_summary.json`, `.md`, and search log report E1 `complete_underpowered`; only 2 baseline closed trades intersect the gamma-proxy date set, all gamma variants collapse to 0 active trades, MinTRL/PSR and big-day dependency are blocked by insufficient observations, no paid data/network was used, and strategy use/paper trading remain forbidden |
| H-G1.23 | Decide H-G1 after underpowered strategy ablation: park, write a separate sample-expansion decision, or return to News-Unblock N.7 | S | RISK | Complete: `experiments\h_g1_post_ablation_decision.json` and `docs\H_G1_POST_ABLATION_DECISION.md` park H-G1 pending a separate sample-expansion plan, return active work to News-Unblock N.7, and confirm no gamma filter, paid data from H-G1.22 alone, paper trading, or true net-gamma claim is allowed |
| H-G1.24 | Pre-register H-G1 sample-expansion requirements before any further gamma data or ablation work | M | RISK | Complete: `experiments\h_g1_sample_expansion_plan.json` and `docs\H_G1_SAMPLE_EXPANSION_PLAN.md` lock the no-paid local-cache overlap scan, MinTRL/PSR feasibility gate, targeted cost gate, and new-preregistration requirement. `scripts\validate_h_g1_sample_expansion_plan.py` passes and confirms the artifact does not approve paid data, strategy use, paper trading, or true net-gamma claims |
| H-G1.24a | Run the no-paid local-cache overlap scan required by H-G1.24 | M | RISK | Complete and blocked: `reports\diagnostics\h_g1_local_cache_overlap_scan.json` found 0 additional no-paid gamma-ready baseline trade dates beyond the current 2-date intersection (`2023-10-27`, `2024-12-18`). `scripts\validate_h_g1_local_cache_overlap_scan.py` passes and confirms no network, no paid data, no strategy PnL, no paper-trading approval, and no true net-gamma claim |

**Track complete when**: Complete through H-G1.24a. H-G1 side-aware data-validity diagnostic passes under policy id `h_g1_required_bucket_policy_v3_side_aware`, and the preregistered strategy ablation has run, but H-G1 remains E1 proxy-validity/underpowered strategy-ablation evidence only. H-G1 is parked, not strategy-ready. The no-paid overlap scan did not find additional local overlap, so H-G1 must not proceed to metadata cost check, paid data, new ablation, strategy use, paper trading, or true net-gamma claim from the current artifacts.

---

## Track H-L1 / H-L2: LLM Measurement Design
**Goal**: Prepare clean LLM research without running live prompt experiments before real timestamp-clean news exists.
**Dependencies**: Track P0; real news archive for execution

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| L.1 | Create `docs\LLM_MEASUREMENT_EXPERIMENT_DESIGN.md` for H-L1 | M | RISK | Complete: design covers prompt families, masking, repeated calls, stability metrics, contamination probe, cost/latency, and no Go/No-Go-only framing |
| L.2 | Create `docs\H_L2_PRICE_INPUT_DESIGN.md` as a separate dormant branch | M | RISK | Complete: design enforces chronological price/quote inputs and separation from news/sentiment |
| L.3 | Add pointers from existing Exp07 docs to the new H-L1/H-L2 designs | S | OK | Complete: `docs\EXP07_PROMPT_REDESIGN.md` and `backtest_experiments_plan.md` point to H-L1/H-L2 and prohibit live LLM research before timestamp-clean news |
| L.4 | Keep GDELT/new-news provider work blocked on source policy | S | RISK | Complete: `docs\M6_NEWS_SOURCE_DECISION_NOTE.md` remains the controlling source-decision note and no live LLM or new provider action was taken |
| L.5 | Separate event-risk proxy work from exact LLM/news research | S | RISK | Complete: `docs\HYPOTHESIS_DATA_RESOLUTION_AUDIT.md` allows macro-calendar/event-label proxy work while preserving the blocker on live LLM prompt research until real timestamp-clean news exists. Proxy work must not be called LLM/news evidence |
| L.6 | Pre-register a macro-event proxy baseline for H-L1 before waiting on GDELT again | S | RISK | Complete: `experiments\h_l1_macro_event_proxy_preregistration.json` and `docs\H_L1_MACRO_EVENT_PROXY_PREREGISTRATION.md` define a deterministic macro-event/VIX/regime baseline using existing timestamp-clean macro calendar and VIX/VXV labels. The preregistration states this is not LLM/news evidence, cannot validate an LLM gate, and forbids live LLM calls, GDELT retry, paid news providers, broker requests, paper trading, operational validation, real-money trading, and E2 claims |
| L.7 | Run the macro-event proxy baseline with local data only | M | RISK | Complete: `scripts\run_h_l1_macro_event_proxy_baseline.py` generated `reports\experiments\h_l1_macro_event_proxy_baseline_summary.json`, `.md`, and `reports\experiments\search_logs\h_l1_macro_event_proxy_baseline_search_log.jsonl`. Result is E1 `ยังสรุปไม่ได้` and explicitly `not_llm_news_evidence=true`: no-risk minus macro-only avg trade PnL is `23.375`, macro-only trade days average `-10.56`, and no-macro/no-VIX-risk trade days average `12.815`. `scripts\validate_h_l1_macro_event_proxy_baseline_summary.py` and `tests\test_h_l1_macro_event_proxy_baseline.py` pass. No network, paid data, GDELT retry, broker request, LLM call, paper trading, operational validation, or real-money approval was used |
| L.8 | Lock post-baseline decision before any real-news or LLM execution | S | RISK | Complete through the shared H-A2/H-L1 decision: `experiments\h_a2_h_l1_post_proxy_decision.json` records that L.7 is only deterministic macro/VIX baseline evidence, not LLM/news evidence. Future H-L1 scores must add information beyond this baseline; live LLM/news research remains blocked until real timestamp-clean news cases exist |
| L.9 | Register H-L3 market-outcome text filtering as a separate hypothesis from semantic prompt measurement | S | RISK | Complete: registry defines SPY-specific chronological labels, quantitative/semantic/market-based controls, separate Sub-System A/B ablations, validation-frozen thresholds, and blocks execution until timestamp-clean news plus a label design exist |

**Track complete when**: Design remains complete, live LLM research has not run without real timestamp-clean news cases, and macro-event proxy work is either completed as E1 non-LLM evidence or explicitly parked without being confused with LLM/news evidence.

---

## Track News-Unblock
**Goal**: Unblock real timestamp-clean news cases without treating provider changes as automatic.
**Dependencies**: Track P0

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| N.1 | Preserve GDELT cooldown discipline after HTTP 429 pressure | S | OK | Command plan continues to record blocked/not-attempted/cooldown status |
| N.2 | Evaluate GDELT bulk raw-file path as a possible free archive route | M | RISK | Complete: `docs\GDELT_BULK_RAW_SOURCE_DECISION_NOTE.md` compares bulk raw files with current GDELT API path and requires a no-download manifest/size probe before any bulk data pull |
| N.3 | Stop for explicit user direction before any new paid news provider or new access method capture | S | RISK | No new provider artifacts exist without explicit user direction |
| N.4 | Build the no-download GDELT bulk raw-file manifest/size probe | M | RISK | Complete: `reports\news_gdelt_bulk_raw_manifest.json` maps 71 candidate days to 6,887 expected GKG files, 6,883 master-list matches, 4 missing files, and about 48.28GB compressed if broadly downloaded; no raw archive file was downloaded |
| N.5 | Run one-file GKG parser probe before any broad raw-file download | M | RISK | Complete with blocker: `reports\news_gdelt_gkg_one_file_parser_probe.json` parsed 605 rows from one small GKG file; source and HTTPS URL map, but headline is blocked, publication/fetch timestamps are surrogate-only, and topic mapping is not pre-registered |
| N.6 | Choose and test a narrow GKG enrichment or DOC API join path | M | RISK | Complete as scaffold only: `reports\news_gdelt_doc_api_enrichment_scaffold.json` selects `gdelt_doc_api_topic_requery_after_cooldown`, proves `DOC API title/seendate/domain/url -> snapshot CSV -> canonical news_item import` on a staging output root, preserves GKG blockers, uses 0 network calls and 0 paid cost, and remains `scaffold_pass_real_archive_blocked` |
| N.7 | Run one narrow real DOC API retry after cooldown clears | M | RISK | Attempted and blocked, then guardrail hardened: one narrow retry for `2023-04-14` returned a non-JSON GDELT response, wrote `reports\news_gdelt_capture_status\2023-04-14.json`, and no CSV/import occurred. `scripts\plan_gdelt_news_capture_commands.py` now reports top-level `status=blocked_cooldown` and `live_retry_allowed=false` whenever retry pressure is `cooldown_recommended`. `scripts\plan_exp07_real_news_cases.py` now inherits that cooldown state, so `reports\experiments\exp07_real_news_case_plan.json` reports `collection_status=blocked_cooldown`, `gdelt_plan_status=blocked_cooldown`, and `gdelt_live_retry_allowed=false` instead of stale `ready_to_collect` wording. The latest no-network command-plan/readiness rerun still reports 6 blocked status files, next unattempted day `2023-04-17`, 65 not-attempted days, and `cooldown_recommended`; no live GDELT retry was made |

| N.8 | Evaluate alternative timestamp-clean real-news source paths instead of waiting only on GDELT | M | RISK | New user decision. Verification: source-decision artifact compares at least free/low-cost feasible alternatives against GDELT, requires real headline/body availability, publication timestamp, fetch/availability timestamp, decision-time discipline, parser/import path, licensing notes, and no live LLM calls |

**Track complete when**: The project either has real timestamp-clean news cases or a documented blocker/source decision. Current status: documented blocker/source decision exists, and one N.7 retry is now blocked by upstream GDELT response quality; real news remains blocked.

---

## Track Acceptance And Operations
**Goal**: Convert E2 evidence, if any, into operational validation without making real-money claims.
**Dependencies**: At least one strategy hypothesis reaches E2 or an E0 operational dry-run is explicitly chosen.

| # | Task | Effort | Risk | Verification |
|:--|:-----|:------:|:----:|:-------------|
| A.1 | Add `scripts\evaluate_research_acceptance.py` | L | RISK | Complete: `reports\research_acceptance_evaluation.json` returns `blocked` because no strategy hypothesis has E2+ evidence, research readiness is still blocked, `paper_trading_allowed=false`, `operational_validation_allowed=false`, and `real_money_allowed=false`; script is wired into `scripts\run_fixture_pipeline.py` |
| A.2 | Require MinTRL/PSR/DSR, big-day survival, implementable PnL, regime coverage, and benchmark comparison for E2 | M | RISK | Complete: `scripts\evaluate_research_acceptance.py` now checks E2+ candidate evidence artifacts for chronological validation/no OOS tuning, resolved MinTRL/PSR/DSR handling, implementable PnL plus cost drag, big-day dependency survival, regime coverage or explicit scope restriction, and benchmark/drawdown comparison; tests reject E2 labels with missing gate evidence |
| A.3 | If E2 exists, validate paper/dry-run tickets and operator workflow | L | RISK | Dry-run config has no live transmit and rejects entry market orders, undefined risk, missing forced close, and excessive buying power |
| A.4 | Keep real-money launch blocked | S | RISK | Complete: `scripts\audit_real_money_launch_gate.py` reads research acceptance, launch checklist, IBKR config, and kill switch, writes `reports\real_money_launch_gate_audit.json/.md`, and currently reports `blocked` with `real_money_allowed=false`; the check is wired into `scripts\run_fixture_pipeline.py` |

**Track complete when**: Either a strategy hypothesis is approved for operational validation with scope restrictions, or the project remains blocked with exact missing research gates.

---

## Execution Notes
- **Active track**: H-A2 has moved past two bounded exact replays into mechanism-first revision. H-A2.58 combined the `2025-02-11` and `2025-05-05` exact replays: two candidates, total `mid_pnl=-50.00`, total `implementable_pnl=-59.12`, both negative after implementable costs, under-sampled/underpowered, and no E2/paper/operational/real-money approval. H-A2.59 reframed H-A2 as a conditional continuation hypothesis, H-A2.60 diagnosed that both directionally correct candidates still lost because the option spread did not reach the long strike after costs, H-A2.61 pre-registered a breakeven-aware train-only rule family, H-A2.62 confirmed the current train artifacts are not enough to lock a true breakeven-aware option rule, and H-A2.63 now converts that blocker into a targeted data/regime expansion plan. H-L1 has a deterministic macro/VIX baseline that future real-news or LLM scores must beat. News-Unblock remains blocked for exact H-L1 live prompt research by GDELT response/cooldown after the `2023-04-14` retry; live LLM research is still forbidden until real timestamp-clean news cases exist. H-B2.5 falsification review is complete and keeps the current grid parked/not resurrected, H-G1 is parked after H-G1.24a found no additional no-paid overlap, and Acceptance A.1/A.2/A.4 machine-report that no hypothesis is approved for operational validation or real-money launch.
- **Immediate next safe action**: Wait for user approval of the H-A2 2022 H2 stress-plan ceiling `$11.726392`. If approved, execute only stage 1: verify the IBKR `$1.50` Network B account price/API readiness and acquire the 13 October SPY bar dates. Run the trade-density checkpoint before any September option purchase. No broad calendar buying is allowed.
- **Hypothesis-specific paid constraints**: The DD freeze no longer blocks acquisition. H-G1 OI dates and the 2022 H2 top-VIX months are the next approved candidates under the section 5 decision tree, but each pull still requires a current live estimate, selected-key budget check, named hypothesis gap, and any experiment-specific preregistration. Missing 2022 SPY underlying bars remains a separate exact H-A2 replay constraint. Any new provider still requires clear user direction.
- **Current cost basis**: Existing Databento working usage remains `$120.494368` against the legacy `$125` guard. User-added env keys `DATABENTO_API_MO` and `DATABENTO_API_AI` are available as one approved `$200` research pool, with each individual key capped at `$100`. Future paid Databento work must choose and log the selected key env name, estimate cost first, and keep the selected key and combined MO/AI usage under cap.
- **Risk checkpoints**: Before every paid pull, after every validator/audit change, before H-A2 stress purchase, before H-G1 12-date OI purchase, before any LLM call, before acceptance, and before paper/dry-run.
- **Proceed-by-default policy**: As of 2026-07-06, normal in-plan SPY-only Higanbana work must proceed without approval prompts. State the intended change, affected files, and verification path, then continue. This includes multi-file edits, experiment scaffolds, local scripts/tests/audits, no-paid data processing, already-approved providers inside the active cost guard, coherent project-control document updates, git status/commit/push work requested by the user, research-log maintenance, and verification-loop runs. Do not pause only because a task changes more than 3 files, creates or updates experiment artifacts, updates `PROJECT_BRAIN.md` / `IMPLEMENT_PLAN.md` / `AGENTS.md`, writes a research log, pushes a requested git update, or runs tests/audits. Ask a question only when required information is missing and cannot be inferred safely. Hard-stop boundaries remain only for: new paid provider, paid action that would breach the active cost guard, broker/order transmission, real-money launch, destructive filesystem/git operation, or scope change beyond SPY. These are out-of-plan safety boundaries, not approval prompts for ordinary work.
- **Archive policy**: Before replacing this plan again, archive the current `IMPLEMENT_PLAN.md` under `Backup_IMPLEMENT_PLAN\` and state why.
- **Verification loop**: After each track-level implementation, run `python -m unittest discover -s tests`; for scaffold-impacting changes also run `python scripts\run_fixture_pipeline.py` and relevant readiness audits.
