build/
logs/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# Local secrets. Keep real values in user/machine environment variables.
.env
.env.*
!.env.example
*.pem
*.key

# Paid/raw market data and generated local datasets are not GitHub artifacts.
data/

# Research logs are published with the main Higanbana repository.

# Optional machine-local path manifest. Never store credentials here.
config/machine.json

# Large generated debug streams.
*.stderr.log
*.stdout.log
